# Review Report: T2-T4

## Scope

- T2: 新增 pricing facade 测试。
- T3: 实现 fides-bff pricing facade。
- T4: 完成本地验证、runtime smoke 和 evidence。

## Files Reviewed

- `apps/fides-bff/internal/biz/pricing.go`
- `apps/fides-bff/internal/service/pricing.go`
- `apps/fides-bff/internal/data/quote_client.go`
- `apps/fides-bff/internal/server/http.go`
- `apps/fides-bff/internal/server/http_test.go`
- `apps/fides-bff/internal/data/quote_client_test.go`
- `apps/fides-bff/internal/conf/conf.go`
- `apps/fides-bff/configs/config.yaml`
- `apps/fides-bff/.env.example`
- `apps/fides-bff/cmd/fides-bff/config_loader.go`
- `apps/fides-bff/cmd/fides-bff/main.go`
- `apps/fides-bff/cmd/fides-bff/wire.go`
- `apps/fides-bff/cmd/fides-bff/wire_gen.go`

## Checklist

| Item | Result | Evidence |
|---|---|---|
| `/api/v1/pricing/quotes` 受 LEN-22 AuthFilter 保护 | PASS | `isProtectedPath` 已覆盖 `/api/v1/pricing/`，新增 handler 在同一路径下 |
| applicantId 只来自 principal context | PASS | service 从 `bffkit.PrincipalFromContext` 读取，测试覆盖攻击者 header 被忽略 |
| 下游传播 `x-applicant-id` | PASS | server test 和 runtime smoke 均验证 |
| 下游传播 `traceparent`/`tracestate` | PASS | server test、data client test 和 runtime smoke 均验证 |
| BFF 不实现报价算法、不访问 quote DB | PASS | biz 只定义 `QuoteClient` 端口，data 只做 HTTP 调用 |
| 422 和 502 错误映射符合 Jira | PASS | `PricingError` 映射和测试覆盖 |
| decimal 语义未被 float 计算污染 | PASS | handler 保留 `json.RawMessage`，BFF 不计算、不格式化金额 |
| Config surface 与 LEN-135 边界清楚 | PASS | 仅新增 quote client 配置面，runtime 注入留给 LEN-135 |

## Findings

无未关闭 P0/P1/P2 问题。

## Verification

见：

- `requirements/LEN-132/evidence/local-verification.md`
- `requirements/LEN-132/evidence/runtime-smoke.md`

## Residual Risk

`make generate` 受本机 Wire Go 版本限制失败，已手工同步 `wire_gen.go` 并通过 `go test ./...`、`go vet ./...`、`go build ./cmd/fides-bff`。后续 CI 或本机工具升级后可重新运行 `make generate` 验证无 diff。
