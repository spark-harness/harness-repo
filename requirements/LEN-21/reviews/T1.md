---
requirement_id: "LEN-21"
task_id: "T1"
reviewer: "spark-code-review (5 dimension checkers + aggregation)"
base_revision: "business-repo@9298274; harness-repo@7c8d363"
diff_scope: "T1 新增文件：business-repo fides-bff 服务 + Go CI；harness-repo service-matrix 登记 + tasks.json T1 状态"
conclusion: "ready-for-gate"
updated_at: "2026-06-16"
---

# Code Review Report

## Scope

- Repository: business-repo（新服务 `services/backend/fides-bff/` + `.github/workflows/fides-bff-ci.yml`）、harness-repo（`.service-matrix/dependencies.yaml`、`requirements/LEN-21/tasks.json`）
- Base revision: business-repo@9298274（master）、harness-repo@7c8d363；本切片所有业务文件均为新增（无历史版本）
- Changed files: 20 个（go.mod/go.sum、Makefile、README.md、.golangci.yml、.gitignore、configs/config.yaml、cmd/fides-bff/{main,wire,wire_gen}.go、internal/{conf,biz,service,server}/*.go、CI workflow）+ service-matrix 登记 + tasks.json
- Task ID: T1（fides-bff 可运行 Kratos 骨架 + `/api/v1/health` + 本地一键运行 + Go CI + 服务矩阵登记）

## Findings

按 severity 排序。无 P0/P1。

| Severity | Dimension | Location | Issue | Impact | Required Fix | Status |
|---|---|---|---|---|---|---|
| P2 | 架构边界 | internal/biz（原 biz.go） | `biz`（domain+application）导入 `google/wire` DI 框架，违反「domain/application 禁止依赖 DI 框架」 | 削弱内层无框架保证；本服务为仓内首个 Go 服务，会成为后续模板 | 将 ProviderSet 移出内层，统一在 bootstrap `wire.go` 直接列出构造函数 | **fixed**：删除 biz.go/service.go/server.go；wire.go 直接 `wire.Build(biz.NewHealthUsecase, service.NewHealthService, server.NewHTTPServer, newApp)`；已验证 internal/ 无 wire 导入 |
| P3 | 架构边界/契约 | internal/service/health.go:31 | 成功响应硬编码字面量 `200` | 与 T2 错误信封/状态映射的命名常量不一致 | 改用 `net/http.StatusOK` | **fixed** |
| P3 | 数据与并发 | cmd/fides-bff/main.go `newApp` | 未设置 `kratos.StopTimeout`，优雅停机使用未限时 context | T1 无慢请求无害；T2+ 接入下游后在飞请求可能阻塞停机 | 增加 `kratos.StopTimeout` | **fixed**：设为 10s |
| P3 | 安全与错误处理 | .github/workflows/fides-bff-ci.yml:22,26,59 | GitHub Actions 固定到浮动大版本 tag 而非 commit SHA | 仅 build/lint、无 secrets，供应链风险低 | 第三方 action 钉到 SHA（属团队级 CI 约定） | **accepted/deferred**：无 secrets，SHA 钉版应作为团队级统一决策，不在 T1 单方面引入 |
| P3 | 安全/数据 | cmd/fides-bff/main.go:55-72 | 启动失败用裸 `panic` 而非结构化日志退出 | 仅观感；fail-fast 行为正确，是 Kratos 模板惯例 | 可选改 `log.Fatalf` | **accepted**：Kratos 惯例，fail-fast 正确 |
| P3 | 追溯与范围 | requirements/LEN-21/tasks.json:11 | T1 scope 文案列出 `internal/{...,data}`，实现按裁剪原则未建 `data/` | 文案与交付树轻微不一致 | 可选收紧 scope 文案标注「data 延后」 | **accepted**：裁剪决策在 design.md 开放问题 3 与 README 已明确记录，正确做法 |
| P3 | 架构边界/测试 | internal/service/health.go | 入站适配层无独立映射测试 | 低（单字段透传、无分支，已被 http_test.go 端到端覆盖） | T2 出现真实分支时补 service 层测试 | **accepted**：骨架阶段透传方法，符合 testing.md「无业务分支可放宽」 |

## Dimension Coverage

| Dimension | Checker | Result | Checked Scope |
|---|---|---|---|
| 追溯与范围 | code_review_traceability_checker | findings（1×P3，已 accepted） | R1/R5、AC1/AC5、BR1；确认无 T2–T4 范围泄漏（无错误信封/幂等/可观测/下游 gRPC） |
| 契约兼容 | code_review_contract_checker | findings（1×P3，已 fixed） | 无 .proto/生成物变更；HTTP `/api/v1/health` 为新增 JSON 契约，符合 BR1；protobuf/grpc 仅为 kratos 间接依赖；idl_impact=no 与 design 一致 |
| 数据与并发 | code_review_data_concurrency_checker | findings（2×P3，1 fixed 1 accepted） | 无写路径/事务/共享可变态/无界循环；信号驱动优雅停机；新服务回滚可逆，与 design 一致 |
| 安全与错误处理 | code_review_security_error_checker | findings（2×P3，1 deferred 1 accepted） | 唯一入口 `/api/v1/health` 无入参；无凭证/PII；health 不泄漏内部信息；CI 无 secrets/无注入面 |
| 架构边界 | backend_architecture_reviewer | findings（1×P2 已 fixed + 2×P3） | 依赖方向 service→biz、biz 无框架（修复后）；DTO/domain 边界正确；Kratos↔干净架构映射成立；data/ 裁剪有据 |
| 测试价值与复杂度 | code_review_reporter（自查） | no findings | 测试断言真实行为（注入 version 透传 + 路由挂载于 /api/v1 + 200 JSON），非「代码被调用」；无构建产物混入（已移除误生成的二进制）；无重复逻辑 |

## Tests Inspected

- `internal/biz/health_test.go::TestHealthUsecase_Check_reportsOkWithInjectedVersion` — PASS（注入 version 透传、status=ok）
- `internal/server/http_test.go::TestHTTPServer_Health_returnsStatusAndVersion` — PASS（GET /api/v1/health → 200 + JSON {status,version}，验证 /api/v1 前缀挂载）
- 命令（修复后复跑全绿）：`gofmt -l .`（clean）、`go vet ./...`（ok）、`go test ./... -count=1`（biz+server PASS）、`golangci-lint run ./...`（0 issues）
- 运行时冒烟：`go run ... -conf configs/config.yaml` 后 `curl /api/v1/health` → `{"status":"ok","version":"v0-t1-test"}` HTTP 200；优雅停机日志正常

## Open Questions

- CI Actions 是否采用 SHA 钉版作为团队级统一约定（当前 P3 deferred）。建议在首个含 secrets 的 workflow 前由团队拍板。

## Residual Risk

- 仓内首个 Go/Kratos 服务，CI 仅在本地等价命令验证通过；GitHub Actions 实际执行需首次 PR 触发后确认（runner 上 setup-go 读取 go.mod=1.24、golangci-lint v2.11.4）。
- T2–T4 的横切（错误信封/幂等/可观测/下游 gRPC 映射）尚未实现，属既定后续切片，非本切片缺陷。

## Conclusion

`ready-for-gate`：无未关闭的 P0/P1。P2 与两个低成本 P3（StatusOK、StopTimeout）已在本切片修复并复验全绿；其余 P3 为有据 accepted/deferred。

本报告不是门禁结论。阶段推进仍以 Janus 门禁 JSON 和人工审批为准。
