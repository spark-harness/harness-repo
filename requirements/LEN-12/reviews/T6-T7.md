---
requirement_id: "LEN-12"
task_id: "T6-T7"
reviewer: "Codex"
base_revision: "origin/master"
diff_scope: "business-repo/services/backend/applicant-api Redis/JDBC runtime adapters, token service, runtime config, service tests, and LEN-12 evidence updates"
conclusion: "ready-for-gate"
updated_at: "2026-06-20T17:51:00+08:00"
---

# Code Review Report

## Scope

- Repository: `business-repo`, `harness-repo`
- Base revision: `origin/master`
- Changed files:
  - `business-repo/services/backend/applicant-api/**`
  - `harness-repo/requirements/LEN-12/tasks.json`
  - `harness-repo/requirements/LEN-12/evidence/applicant-api-tests.md`
- Task ID: `T6`, `T7`

## Findings

| Severity | Dimension | Location | Issue | Impact | Required Fix | Status |
|---|---|---|---|---|---|---|
| P2 | 安全与错误处理 | `services/backend/applicant-api/src/main/java/com/spark/applicant/infrastructure/auth/HmacTokenService.java` | 当前 HMAC token 是服务端签名的 opaque token，没有 issuer、audience、subject claim 或标准 JWT 解析能力。 | 已满足本需求的短期 token 签发和服务端 refresh token 状态保存，但若后续 BFF 要在本地解析 token，需要另行定义 token 格式和验证端口。 | 在 BFF 接入或资源权限需求中明确 token 消费方式；如需要本地解析，补 JWT/claims/verification 设计和测试。 | open |

Severity 口径：

- `P0`：正确性、数据丢失、安全或契约破坏。
- `P1`：大概率生产 Bug、缺少必需证据或门禁阻塞项。
- `P2`：可维护性、测试、可观测性或灰度风险。
- `P3`：不应阻塞的小问题。

## Dimension Coverage

| Dimension | Checker | Result | Checked Scope |
|---|---|---|---|
| 追溯与范围 | code_review_traceability_checker | skipped | 多-agent 工具要求用户显式授权后才能启动；主线程检查了 `requirement.md`、`design.md`、`tasks.json` 与 T6/T7 scope。 |
| 契约兼容 | code_review_contract_checker | skipped | 子代理未启动；主线程检查了本次改动不修改 protobuf 契约，只消费既有 applicant generated contract。 |
| 数据与并发 | code_review_data_concurrency_checker | skipped | 子代理未启动；主线程检查了 Redis TTL、cooldown key、refresh token hash key、idempotency conflict 和 JDBC phone unique fallback。 |
| 安全与错误处理 | code_review_security_error_checker | skipped | 子代理未启动；主线程检查了生产 test provider 禁用、HMAC secret fail-fast、token hash Redis key、disabled provider error mapping、敏感字段日志规避和 trace error_code 标记。 |
| 架构边界 | backend_architecture_reviewer | skipped | 子代理未启动；主线程检查了 application port 与 infrastructure adapter 依赖方向，以及 ArchUnit 结果。 |
| 测试价值与复杂度 | code_review_reporter | findings | `ApplicantAuthUseCaseTest`、`RedisAuthRepositoryTest`、`JdbcApplicantRepositoryTest`、`ApplicantAuthConfigurationTest`、gRPC/health/smoke/architecture tests。 |

`skipped` 原因：当前 multi-agent 工具说明禁止在用户未显式要求子代理时派生 review checker；本报告不把 skipped 维度视为独立 checker 已通过。

## Tests Inspected

- `mvn test` in `business-repo/services/backend/applicant-api`: PASS, 29 tests.
- Failure baseline before implementation: `mvn test` failed at test compile because JDBC/Redis dependencies, `ApplicantAuthProperties`, Redis repository adapters, JDBC applicant repository, and runtime policy validation did not exist.
- After adding Redis starter, first full test run failed because Actuator Redis health made default in-memory smoke test DOWN without local Redis. Fixed by disabling Redis health in default profile and enabling it in `prod`.
- After adding JDBC dependencies, smoke coverage was extended to assert the default in-memory runtime does not create a DataSource. `redis-jdbc` mode now owns explicit DataSource/JdbcTemplate creation.
- T7 added low-cardinality `applicant.auth.requests` and `applicant.auth.duration` metrics, current-span `error_code` marking, trace/span fields in sensitive-safe logs, Redis timeout profile config, and runtime fail-fast tests for prod misconfiguration.
- `mvn clean` in `business-repo/services/backend/applicant-api`: PASS; Maven `target/` output removed after verification.

## Open Questions

- Whether downstream BFF will treat access tokens as opaque tokens or require standard JWT claims is outside LEN-12 and should be resolved in the BFF integration requirement.
- The production schema migration tool is not defined in this repo. `initialize-schema` is documented as local/temporary only.

## Residual Risk

- T6 is complete for the approved backend implementation slice.
- T7 is complete for service-code runtime safety and observability hardening. Dashboard, alert, and production collector wiring remain deployment follow-up, not service-code blockers.
- `service-repo-check.gate.json` remains BLOCKED because `impact-analysis.md` lacks approved frontmatter fields; this report does not override Janus gate state.

## Conclusion

- `ready-for-gate`：无未关闭的 P0/P1。

本报告不是门禁结论。阶段推进仍以 Janus 门禁 JSON 和人工审批为准。
