---
requirement_id: "LEN-12"
task_id: "T4-T5"
reviewer: "Codex"
base_revision: "origin/master"
diff_scope: "business-repo/services/backend/applicant-api; idl-java-repo applicant generated contracts and protobuf runtime version"
conclusion: "ready-for-gate"
updated_at: "2026-06-19T19:22:00+08:00"
---

# Code Review Report

## Scope

- Repository: `business-repo`, `idl-java-repo`
- Base revision: `origin/master`
- Changed files:
  - `business-repo/services/backend/applicant-api/**`
  - `idl-java-repo/pom.xml`
  - `idl-java-repo/src/main/java/com/vesta/spark/applicant/v1/**`
  - `idl-java-repo/src/main/grpc-java/com/vesta/spark/applicant/v1/**`
- Task ID: `T4`, `T5`

## Findings

| Severity | Dimension | Location | Issue | Impact | Required Fix | Status |
|---|---|---|---|---|---|---|
| P2 | 数据与并发 | `services/backend/applicant-api/src/main/java/com/spark/applicant/infrastructure/auth/*` | 当前实现使用内存适配器承载 applicant、OTP、token 与幂等状态，尚未接入设计要求的 Redis 和 durable applicant persistence。 | 行为测试可证明业务规则，但服务重启后状态会丢失，不能作为生产可用实现。 | 在 `T6` 中替换为 Redis-backed runtime state 与 durable applicant repository，并补集成测试。 | open |
| P2 | 安全与可观测性 | `services/backend/applicant-api/src/main/java/com/spark/applicant/infrastructure/auth/SimpleTokenService.java` | token 当前是测试级随机字符串，未实现签名、issuer、audience、secret 引用、token hash 存储和脱敏日志策略。 | 可满足 T4/T5 行为测试，但不能作为正式身份令牌实现。 | 在 `T6/T7` 中实现正式 token 服务、配置校验、脱敏与错误码可观测性。 | open |

Severity 口径：

- `P0`：正确性、数据丢失、安全或契约破坏。
- `P1`：大概率生产 Bug、缺少必需证据或门禁阻塞项。
- `P2`：可维护性、测试、可观测性或灰度风险。
- `P3`：不应阻塞的小问题。

## Dimension Coverage

| Dimension | Checker | Result | Checked Scope |
|---|---|---|---|
| 追溯与范围 | code_review_traceability_checker | skipped | 子代理工具规则要求用户显式授权后才能启动；主线程检查了 `requirement.md`、`design.md`、`tasks.json` 与 T4/T5 diff。 |
| 契约兼容 | code_review_contract_checker | skipped | 子代理未启动；主线程检查了新增 applicant generated contract 依赖和 protobuf runtime 4.35.1 对齐。 |
| 数据与并发 | code_review_data_concurrency_checker | skipped | 子代理未启动；主线程检查了 OTP 冷却、错误次数、锁定、幂等回放、refresh TTL 与内存适配器边界。 |
| 安全与错误处理 | code_review_security_error_checker | skipped | 子代理未启动；主线程检查了 gRPC error mapping、OTP hash、token 替身与敏感信息风险。 |
| 架构边界 | backend_architecture_reviewer | skipped | 子代理未启动；主线程检查了 Clean Architecture 目录、port 方向和 ArchUnit 结果。 |
| 测试价值与复杂度 | code_review_reporter | findings | `ApplicantAuthUseCaseTest`、`ApplicantAuthGrpcAdapterTest`、`HealthHttpAdapterTest`、`ApplicantApiApplicationSmokeTest`、`DomainLayerArchitectureTest`。 |

`skipped` 原因：当前工具约束禁止在用户未明确要求 sub-agent 时派生 review checker；本报告不把 skipped 维度视为独立 checker 已通过。

## Tests Inspected

- `mvn test` in `business-repo/services/backend/applicant-api`: PASS, 19 tests.
- Test-first failure before implementation: `mvn test` failed at compile because applicant-api production classes and same-branch applicant generated contracts were missing.
- Local dependency setup required before service test:
  - `mvn install -DskipTests` in `business-repo/packages/spring-starter`: PASS.
  - `mvn install -DskipTests` in `idl-java-repo`: PASS.
- Initial gRPC test failure found protobuf gencode/runtime mismatch: generated applicant code used protobuf 4.35.1 while `idl-java-repo/pom.xml` pinned 4.35.0. Fixed by aligning runtime to 4.35.1 and re-running `mvn test`.

## Open Questions

- T6 still needs the final persistence decision for applicant identity: database-backed repository or another durable store. Current in-memory repository is only a T4/T5 skeleton implementation.
- T6/T7 still need production token signing, secret reference, Redis connection configuration, and production fail-fast behavior for test OTP provider.

## Residual Risk

- T4/T5 are ready for the next gate because they establish the service skeleton and executable behavior expectations, but T6/T7 remain required before production readiness.
- No delegated checker agents were run due to tool policy; if the team requires full stage 4.4 review protocol, rerun this review with explicit sub-agent authorization.

## Conclusion

- `ready-for-gate`：无未关闭的 P0/P1。

本报告不是门禁结论。阶段推进仍以 Janus 门禁 JSON 和人工审批为准。
