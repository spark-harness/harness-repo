# LEN-12 IDL 与生成契约证据

## Scope

- Requirement: `LEN-12`
- Branch: `feature/applicant-api/LEN-12`
- IDL repo: `.worktrees/LEN-12/idl-repo`
- Generated Java repo: `.worktrees/LEN-12/idl-java-repo`

## Contract Change

- Change type: additive new service/RPC/message.
- New proto: `vesta/spark/applicant/v1/auth.proto`
- New service: `vesta.spark.applicant.v1.ApplicantAuthService`
- RPCs:
  - `SendOtp`
  - `VerifyOtp`
  - `RefreshToken`

## Commands

```bash
cd /Users/forest/Code/spark/.worktrees/LEN-12/idl-repo
buf lint
buf breaking --against .git#branch=master
buf generate
```

## Results

- `buf lint`: PASS
- `buf breaking --against .git#branch=master`: PASS
- `buf generate`: PASS

## Generated Outputs

Java generated contracts are generated by the `idl-repo` CI workflow and pushed
to the same branch in `spark-harness/idl-java-repo`, not hand-committed as part
of this LEN-12 branch:

```text
idl-java-repo/src/main/java/com/vesta/spark/applicant/v1/
idl-java-repo/src/main/grpc-java/com/vesta/spark/applicant/v1/ApplicantAuthServiceGrpc.java
```

The current `buf.gen.yaml` also writes Go output, but `LEN-12` does not consume
Go contracts because `fides-bff` is outside this backend scope.

## Compatibility

No existing protobuf package, service, RPC, field number, or field type was
changed. Existing `vesta.spark.user.v1` contracts remain untouched after
discarding protoc patch-version churn in generated Java files.

## Rebase Verification

After rebasing LEN-12 worktrees onto `origin/master` on 2026-06-20, the IDL
checks were rerun from `/Users/forest/Code/spark/.worktrees/LEN-12/idl-repo`:

```bash
buf lint
buf breaking --against .git#branch=master
```

Result: PASS.

For local verification only, the Java generated contract package was installed
from `/Users/forest/Code/spark/.worktrees/LEN-12/idl-java-repo` before running
`applicant-api` tests:

```bash
mvn install -DskipTests
```

Result: PASS.

## Generated Contract Pipeline Note

`idl-repo/README.md` and `.github/workflows/sync-java-idl.yml` define the
generated Java contract handoff: on proto changes, `idl-repo` CI runs
`buf generate`, builds `idl-java-repo`, and pushes generated Java contracts to
the same branch in `spark-harness/idl-java-repo`.

Local verification found that the current remote Java generator emits protobuf
Java gencode `4.35.1`; the CI-generated Java contract artifact must use a
protobuf runtime version that is not older than that gencode version before
`applicant-api` consumes the published package.

## Java Formal Publish Readiness

On 2026-06-20, GitHub Packages was checked for
`com.spark.contract:spark-idl-java`. The package exists, but only version
`0.1.0-SNAPSHOT` is currently published. No formal SemVer Java contract version
exists yet, so `business-repo` cannot satisfy the master-bound contract
dependency scan by changing to an already-published formal version.

To unblock formal contract consumption without hand-pushing generated Java
contracts, `idl-repo` now includes `.github/workflows/publish-java-idl.yml`.
The workflow:

- is triggered by `idl-repo` SemVer / RC tags or manual RC dispatch;
- resolves the frozen IDL ref from the tag or workflow input;
- checks that the target Maven package version does not already exist;
- checks out `spark-harness/idl-java-repo` as the generated Java Maven project;
- runs `buf generate` from the frozen `idl-repo` ref;
- writes the target Maven version for the publish run only;
- aligns `protobuf-java.version` with the generated Java gencode version; and
- publishes `com.spark.contract:spark-idl-java` with `mvn -B deploy`.

Local validation:

```bash
cd /Users/forest/Code/spark/.worktrees/LEN-12/idl-repo
actionlint .github/workflows/publish-java-idl.yml
buf lint
buf breaking --against .git#branch=master
```

Result: PASS.

## Formal Contract Publication

After merging `idl-repo` PR #2, `v0.1.0` was created on IDL merge commit
`b0bedbaf673048132d4eb670edbdbbec9f842874`.

Publication evidence:

- `Publish Go IDL` run `27875158056`: PASS.
- `Publish Java IDL` run `27875158037`: initial attempt failed because the old
  `IDL_JAVA_REPO_TOKEN` secret lacked `read:packages`; after updating the
  secret, rerun job `82493823897` passed.
- GitHub Packages now contains `com.spark.contract:spark-idl-java:0.1.0`.
- Existing snapshot package remains `0.1.0-SNAPSHOT`, but `LEN-12` business
  consumption now uses formal `0.1.0`.

The earlier dry-run in `/private/tmp/len12-java-publish-dryrun` copied the IDL and Java
generated-contract repositories, ran `buf generate`, prepared a temporary
`spark-idl-java` version `0.2.0`, detected protobuf Java gencode `4.35.1`, and
ran:

```bash
mvn -B test
```

Result: PASS.
