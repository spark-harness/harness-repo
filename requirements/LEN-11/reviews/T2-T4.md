---
requirement_id: "LEN-11"
task_id: "T2-T4"
reviewer: "Codex"
base_revision: "origin/master"
diff_scope: "business-repo/apps/fides-web loan-request implementation and harness-repo/requirements/LEN-11 lifecycle evidence"
conclusion: "ready-for-gate"
updated_at: "2026-06-28T07:34:59+08:00"
---

# Code Review Report

## Scope

- Repository: `business-repo`, `harness-repo`
- Base revision: `origin/master`
- Changed files:
  - `apps/fides-web/src/application/loan-request/loan-request-gateway.ts`
  - `apps/fides-web/src/adapters/loan-request/loan-request-controller.ts`
  - `apps/fides-web/src/infrastructure/loan-request/rest-loan-request-gateway.ts`
  - `apps/fides-web/src/infrastructure/loan-request/browser-draft-store.ts`
  - `apps/fides-web/src/presentation/loan-request/loan-request-screen.tsx`
  - `apps/fides-web/src/api/fides-application.tsx`
  - `apps/fides-web/src/app/page.tsx`
  - matching tests under the same feature paths
  - `requirements/LEN-11/**`
- Task ID: T2-T4

## Findings

| Severity | Dimension | Location | Issue | Impact | Required Fix | Status |
|---|---|---|---|---|---|---|
| P1 | 追溯与范围 | `presentation/loan-request/loan-request-screen.tsx` pre-review | Initial implementation called `controller.load()` but did not apply returned draft detail to UI. | AC8 same-draft refill would not work. | Add draft detail type, controller view model, gateway detail parser, screen refill logic, and tests. | closed |

## Dimension Coverage

| Dimension | Checker | Result | Checked Scope |
|---|---|---|---|
| 追溯与范围 | main-thread review, not delegated because user did not request sub-agents | no open findings | AC1-AC10 against changed fides-web files and LEN-11 tasks |
| 契约兼容 | main-thread review, not delegated because user did not request sub-agents | no findings | REST BFF endpoints, headers, no IDL changes |
| 数据与并发 | main-thread review, not delegated because user did not request sub-agents | no findings | idempotency keys, stale quote keying, draft pointer storage |
| 安全与错误处理 | main-thread review, not delegated because user did not request sub-agents | no findings | bearer token memory-only path, sessionStorage contents, error envelope mapping, trace headers |
| 架构边界 | backend_architecture_reviewer | skipped | no backend service code changed |
| 测试价值与复杂度 | main-thread review | no findings | frontend unit/component/infrastructure tests, lint, dependency cruiser, build evidence |

## Tests Inspected

- `pnpm test -- src/infrastructure/loan-request/rest-loan-request-gateway.test.ts src/infrastructure/loan-request/browser-draft-store.test.ts src/adapters/loan-request/loan-request-controller.test.ts src/presentation/loan-request/loan-request-screen.test.tsx src/api/fides-application.test.tsx`
  - PASS: 69 passed, 1 skipped.
- `pnpm test`
  - PASS: 69 passed, 1 skipped.
- `pnpm lint`
  - PASS with one existing warning in `mock-otp-auth-gateway.ts`.
- `pnpm lint:deps`
  - PASS, no dependency violations.
- `pnpm build`
  - PASS.

## Open Questions

- 完整 OTP verify E2E 当前被 `fides-bff -> applicant-api` 的 `applicant_unavailable` 阻塞。第二页所需 pricing/create/patch API 已用临时 HMAC token 验证通过。

## Residual Risk

- 浏览器截图工具 readback 失败，UI runtime evidence 以 Chrome accessibility tree 和组件测试为准。
- `GET /loan-applications/{id}` 回填依赖 BFF detail response shape；该 shape 已按当前 `fides-bff` service 和 tests 确认。

## Conclusion

结论为 `ready-for-gate`。初始 AC8 缺口已修复并补测试；当前无未关闭 P0/P1。

本报告不是门禁结论。阶段推进仍以 Janus 门禁 JSON 和人工审批为准。
