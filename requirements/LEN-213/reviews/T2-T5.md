---
requirement_id: "LEN-213"
task_id: "T2-T5"
reviewer: "forest"
base_revision: "harness-repo origin/master b8835dc; business-repo origin/master 246d1f4"
diff_scope: "harness-repo/requirements/LEN-213 and business-repo/apps/fides-web"
conclusion: "ready-for-gate"
updated_at: "2026-07-06"
---

# Code Review Report

## Scope

- Repository: `harness-repo`, `business-repo`
- Base revision: `harness-repo origin/master b8835dc`; `business-repo origin/master 246d1f4`
- Changed files: LEN-213 lifecycle/evidence/review/gate files and `business-repo/apps/fides-web` server logger, BFF proxy, runtime-config route, tests, ESLint config
- Task ID: T2-T5

## Findings

| Severity | Dimension | Location | Issue | Impact | Required Fix | Status |
|---|---|---|---|---|---|---|
| P0 | 安全与错误处理 | `business-repo/apps/fides-web/src/infrastructure/observability/server-logger.ts` | 外部 `x-request-id` 曾原样进入 JSON 日志。 | 调用方可注入高基数或敏感值进入日志。 | 仅复用长度、字符集和敏感模式校验通过的 request id；否则生成 `req_${randomUUID}`；补 oversized/token/phone-like 测试。 | closed |
| P1 | 契约兼容 | `business-repo/apps/fides-web/src/api/bff-proxy/bff-proxy-route.ts`; `business-repo/apps/fides-web/src/api/runtime-config/runtime-config-route.ts` | 新增错误码曾使用非治理空间字符串。 | 日志错误码语义不可治理、不可稳定聚合。 | 改为 `FIDES-DEPENDENCY-0001`、`FIDES-SYSTEM-0001`，并在 design Error Code Registry 记录语义、重试性、可见性和 owner。 | closed |
| P1 | 安全与错误处理 | `business-repo/apps/fides-web/src/api/bff-proxy/bff-proxy-route.ts` | BFF HTTP 4xx/5xx 曾按 INFO 记录且不含 error_code。 | 下游失败难以通过错误日志和错误码查询。 | `status >= 500` 记录 ERROR，`status >= 400` 记录 WARN，保持 response passthrough。 | closed |
| P1 | 追溯与范围 | `harness-repo/requirements/LEN-213/tasks.json` | T1-T4 曾仍为 `todo`，无法确认当前实现切片。 | 审查无法判断 diff 是否越界。 | 更新任务状态：T1-T4 `done`，T5 收尾后置为 `done`。 | closed |
| P1 | 追溯与范围 | `harness-repo/requirements/LEN-213/tasks.json` | T5 曾缺少 evidence/review/merge-readiness artifact。 | 收尾切片验收无法从文件证明。 | 增加 `evidence/local-verification.md`、本 review 和 merge-readiness gate。 | closed |
| P2 | 数据与并发 | `business-repo/apps/fides-web/src/api/bff-proxy/bff-proxy-route.ts` | BFF proxy fetch 曾无超时边界。 | 下游卡住时 Next.js 请求可能长期占用并发，失败日志不产生。 | 使用 `AbortController` 设置 10s timeout，记录 `TimeoutError` + `FIDES-DEPENDENCY-0001`，不新增重试；补 timeout 测试。 | closed |
| P2 | 安全与错误处理 | `business-repo/apps/fides-web/src/infrastructure/observability/server-logger.ts` | `traceparent` fallback 曾把 upstream parent span 写为当前 `span_id`。 | 日志可能错误关联到不存在的当前 span。 | 仅 active OTel span 写 `span_id`；header fallback 只写 `trace_id` + `request_id`。 | closed |
| P2 | 安全与错误处理 | `business-repo/apps/fides-web/eslint.config.mjs` | console lint guard 曾只覆盖 `console.*` 直接调用。 | `globalThis.console.*` 或 alias 可能绕过统一 logger。 | 增加 `MemberExpression[property.name='console']` 与 `Identifier[name='console']` selector，并保留 logger 文件豁免。 | closed |
| P3 | 安全与错误处理 | `harness-repo/requirements/LEN-213/design.md`; `impact-analysis.md` | 文档曾暗示 traceparent fallback 提取 span_id。 | 后续可能重新引入 fake current span 关联。 | 更新 wording：active span 才写 `span_id`，traceparent fallback 只写 `trace_id`。 | closed |

## Dimension Coverage

| Dimension | Checker | Result | Checked Scope |
|---|---|---|---|
| 追溯与范围 | code_review_traceability_checker | findings closed | requirement、impact、design、tasks、business diff |
| 契约兼容 | code_review_contract_checker | no findings | error-code registry、HTTP surfaces、IDL/generated contract absence |
| 数据与并发 | code_review_data_concurrency_checker | no findings | BFF proxy fetch timeout, no retry, idempotency header passthrough |
| 安全与错误处理 | code_review_security_error_checker | findings closed | request id sanitization, sensitive fields, trace/log correlation, lint guard |
| 架构边界 | backend_architecture_reviewer | skipped | No backend service or Java/Go clean architecture change; frontend dependency gate covers fides-web boundary |
| 测试价值与复杂度 | code_review_reporter | no findings | Test-first failure observed; focused unit/route tests plus full fides-web verification |

## Tests Inspected

- `pnpm test src/infrastructure/observability/server-logger.test.ts src/api/bff-proxy/bff-proxy-route.test.ts`: PASS, 9 tests passed.
- `pnpm test src/api/bff-proxy/bff-proxy-route.test.ts src/api/runtime-config/runtime-config-route.test.ts`: PASS, 4 tests passed.
- `pnpm lint`: PASS, 0 errors; one pre-existing unused-parameter warning.
- `pnpm lint:deps`: PASS, no dependency violations.
- `pnpm test`: PASS, 27 passed / 1 skipped files; 105 passed / 1 skipped tests.
- `pnpm build`: PASS, Next.js compile, TypeScript, page data and route generation passed.

## Open Questions

- None blocking merge.

## Residual Risk

- First version emits stdout JSON only; direct OTEL Logs export remains a future platform decision.
- `pnpm lint` still reports a pre-existing warning in `mock-otp-auth-gateway.ts`; it is unrelated and non-blocking.

## Conclusion

`ready-for-gate`: all P0/P1 findings are closed, local verification passed, and remaining risk is explicitly scoped.
