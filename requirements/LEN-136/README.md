# LEN-136

本需求将 Lendora 从旧 `lendora-sta` 临时环境迁移为 Argo CD 管理的 `dev-1` 和 `sta-1` 双环境。

## 文件

- `requirement.md`：需求、业务规则和验收标准。
- `impact-analysis.md`：影响面、依赖和风险。
- `design.md`：GitOps、Kubernetes、Consul、PostgreSQL、Redis、入口和清理设计。
- `tasks.json`：可验证实施切片。

