# Code Review: T2-T7

## Scope

- Requirement: LEN-41
- Task slice: T2, T3, T4, T5, T6, T7
- Repos reviewed:
  - `idl-repo`
  - `idl-java-repo`
  - `idl-go-repo`
  - `business-repo`
  - `harness-repo`

The configured review sub-agents were not spawned because the current tool policy only permits sub-agent use when the user explicitly asks for delegation. This report records the same review dimensions from the main thread.

## Findings

### Resolved: Formal `spark-idl-java` version now contains the Lendora applicant package

- Dimension: contract compatibility / consumer readiness
- File: `business-repo/services/backend/applicant-api/pom.xml`
- Previous issue: the committed dependency kept `com.spark.contract:spark-idl-java` at `spark.contract.version=0.1.0`, whose published artifact did not contain `com.vesta.lendora.applicant.v1`.
- Resolution: formal IDL tag `v0.2.1` now points to IDL master commit `5ae0eb6d9151477f358932996870e085ce2f8a6b`; Publish Java IDL run `27890705526` published Maven `com.spark.contract:spark-idl-java:0.2.1`; Publish Go IDL run `27890705528` published Go module tag `v0.2.1`; `applicant-api` now uses `spark.contract.version=0.2.1`.
- Verification: `business-repo/services/backend/applicant-api` default `mvn -B -s <temporary-github-settings> test` passed 29 tests against formal `spark-idl-java:0.2.1`; `git merge-base --is-ancestor v0.2.1 origin/master` confirms the formal IDL tag is reachable from `idl-repo` `origin/master`.

## No Findings

- Requirement/design/task traceability: changes map to T2-T7 after the corrected decision that Maven artifact and Go module stay on existing coordinates.
- Protobuf contract shape: applicant RPC names, message names, fields and field numbers are unchanged.
- Generated Java contract: `idl-java-repo` keeps `com.spark.contract:spark-idl-java`, adds `com.vesta.lendora.applicant.v1`, and retains generated `com.vesta.spark.user.v1` classes.
- Generated Go contract: `idl-go-repo` keeps module `github.com/spark-harness/idl-go-repo`, adds `vesta/lendora/applicant/v1`, and retains existing `vesta/spark/user/v1` generated package.
- Backend architecture: `applicant-api` changes are limited to generated contract imports.
- Data/concurrency/rollback: no database, Redis, token, idempotency, transaction, retry or runtime behavior changes were introduced.
- Security/error/observability: no logging, metrics, tracing, auth or error-code behavior changes were introduced.
- Scanner governance: existing `spark-idl-java` and `idl-go-repo` policy remains in place; no new Lendora artifact/module scanner config is needed.

## Tests Inspected Or Run

- `buf lint`
- `buf generate`
- `buf breaking --against .git#branch=master`
- `mvn -B test` in `idl-java-repo`
- `mvn -B install -DskipTests` in `idl-java-repo`
- `go test ./...` in `idl-go-repo`
- `python3 -m unittest tests/test_contract_dependency_scan.py`
- `python3 scripts/contract_dependency_scan.py --mode master --path services/backend/applicant-api/pom.xml`
- `mvn -B install -DskipTests` in `business-repo/packages/spring-starter`
- `mvn -B test` in `business-repo/services/backend/applicant-api` (historical FAIL against formal `0.1.0` before formal Lendora applicant package publication)
- `mvn -B test -Dspark.contract.version=0.1.0-SNAPSHOT` in `business-repo/services/backend/applicant-api` (PASS, 29 tests)
- `mvn -B -s <temporary-github-settings> test` in `business-repo/services/backend/applicant-api` (PASS, 29 tests against formal `spark-idl-java:0.2.1`)
- Publish Java IDL run `27888287900` (PASS, Maven `0.2.0`)
- Publish Go IDL run `27888287898` (PASS, Go tag `v0.2.0`)
- Publish Java IDL run `27890705526` (PASS, Maven `0.2.1`)
- Publish Go IDL run `27890705528` (PASS, Go tag `v0.2.1`)
- `git merge-base --is-ancestor v0.2.1 origin/master` in `idl-repo` (PASS)

## Conclusion

Conclusion: `ready`.

The code changes are coherent and verified against formal generated contract version `0.2.1`. Merge-readiness is no longer blocked by contract publication or release-branch reachability.
