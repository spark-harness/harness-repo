---
requirement_id: "LEN-158"
task_id: "T2-T4"
reviewer: "codex"
base_revision: "business-repo c3ac591; gitops-repo 57e4076; harness-repo fddec57"
diff_scope: "LEN-158 local implementation and GitOps draft"
conclusion: "platform-ready-cutover-pending"
updated_at: "2026-07-04T18:20:45+08:00"
---

# Code Review Report

## Scope

- Repository: `business-repo`, `gitops-repo`, `harness-repo`
- Base revision: `business-repo c3ac591`; `gitops-repo 57e4076`; `harness-repo fddec57`
- Changed files: fides-bff, fides-web, quote-api, origination-api, applicant-api, VSO platform/GitOps, LEN-158 Harness artifacts
- Task ID: T2-T4

## Findings

| Severity | Dimension | Location | Issue | Impact | Required Fix | Status |
|---|---|---|---|---|---|---|
| P1 | Traceability / Data | `gitops-repo/platform/vault-runtime-auth/vault-connection.yaml:10` | `VaultConnection` still pointed to `https://vault.example.invalid`. | VSO cannot sync runtime Secrets; hard-cut Pods would fail if applied. | Replace with approved reachable Vault address before enabling service `VaultStaticSecret` resources, or keep service injection out of merge. | resolved |
| P1 | Traceability / Security | `gitops-repo/platform/vault-runtime-auth/dev-1-auth.yaml:23` | Initial namespace-wide role was too broad for service runtime paths. | Cross-service secret read risk. | Create service-specific VaultAuth/roles/policies and point each `VaultStaticSecret` at its own auth ref. | resolved |
| P1 | Traceability / Data | `gitops-repo/apps/fides/overlays/dev-1/vault-static-secret.yaml:14` | fides-web path initially used `fides/runtime` while design used `fides-web/runtime`. | Migrated Vault values could land at the documented path but not sync. | Align path and design. | resolved |
| P1 | Traceability / Data | `gitops-repo/apps/quote-api/base/consul-config.yaml:1` | Consul KV bootstrap deletion was mixed into pre-smoke GitOps changes. | Rollback boundary violated before dev-1 / sta-1 proof. | Keep bootstrap until T5 environment verification, then delete. | resolved |
| P1 | Data | `gitops-repo/apps/lendora-shared-dependencies/base/applicant-postgres-init.yaml:38` | Shared PostgreSQL init Job was changed in place to final env key. | Job immutability and missing shared-namespace VSO producer could break shared dependency sync. | Keep shared init Secret key unchanged; do not couple shared init to service runtime Secret. | resolved |
| P0 | Security | `business-repo/apps/applicant-api/src/main/resources/application.yml:49` | Applicant token secret had public fallback. | Missing Vault key could make HMAC tokens use a repo-known default. | Remove secret fallback and fail fast for managed profiles. | resolved |
| P0 | Security | `business-repo/apps/fides-bff/configs/config.yaml:37` | BFF token secret had public fallback. | Missing Vault key could allow forged HMAC access tokens. | Remove fallback and validate `AUTH_TOKEN_SECRET` when HMAC mode is enabled. | resolved |
| P1 | Security | `business-repo/apps/fides-bff/internal/observability/otel.go:21` | OTEL enabled with empty endpoint returned no-op. | Missing Secret data could silently disable tracing. | Fail startup when OTEL is enabled without endpoint or configured headers. | resolved |
| P1 | Security | `business-repo/apps/fides-web/src/infrastructure/runtime-config/runtime-config.ts:117` | Browser tracing headers were silently dropped when malformed. | Bad Vault/runtime config could omit trace auth without surfacing error. | Validate `FIDES_BROWSER_TRACING_HEADERS` as non-empty `key=value` pairs. | resolved |
| P2 | Security | `gitops-repo/clusters/lendora-shared/argocd-project-vault.yaml:20` | Vault AppProject used wildcard resource whitelists. | Platform sync authority was broader than necessary. | Restrict whitelist to rendered VSO resources. | resolved |
| P2 | Data | `gitops-repo/apps/quote-api/overlays/dev-1/vault-static-secret.yaml:20` | VSO default `_raw` key appeared in the probe Secret. | Hard-cut runtime Secret would include an extra non-env key. | Set `excludeRaw: true` on every service `VaultStaticSecret` transformation. | resolved |

Severity 口径：

- `P0`：正确性、数据丢失、安全或契约破坏。
- `P1`：大概率生产 Bug、缺少必需证据或门禁阻塞项。
- `P2`：可维护性、测试、可观测性或灰度风险。
- `P3`：不应阻塞的小问题。

## Dimension Coverage

| Dimension | Checker | Result | Checked Scope |
|---|---|---|---|
| 追溯与范围 | code_review_traceability_checker | findings | LEN-158 Harness docs/tasks, business service config, GitOps VSO and app overlays |
| 契约兼容 | code_review_contract_checker | no findings | No protobuf, HTTP route, generated contract, error-code, event schema, or IDL version changes |
| 数据与并发 | code_review_data_concurrency_checker | findings | VSO Secret hard cut, shared PostgreSQL init Jobs, rollback and bootstrap sequencing |
| 安全与错误处理 | code_review_security_error_checker | findings | Secret defaults, OTEL fail-fast, browser tracing headers, VaultAuth blast radius |
| 架构边界 | backend_architecture_reviewer | no findings | BFF/Java config boundaries, service discovery preservation, no Vault dependency in app domain |
| 测试价值与复杂度 | code_review_reporter | findings | Evidence is strong for local code and render checks; live Vault/VSO evidence is still missing |

## Tests Inspected

- `business-repo/apps/fides-web`: targeted vitest, `pnpm lint`, `pnpm test`, `pnpm lint:deps`, `pnpm build` passed.
- `business-repo/apps/fides-bff`: targeted config tests, `go test ./...`, `go vet ./...`, `go build -buildvcs=false ./cmd/fides-bff` passed.
- `business-repo/apps/quote-api`: `mvn -q test spotless:check checkstyle:check spotbugs:check` passed.
- `business-repo/apps/origination-api`: `mvn -q test spotless:check checkstyle:check spotbugs:check` passed.
- `business-repo/apps/applicant-api`: `mvn -q test spotless:check checkstyle:check spotbugs:check` passed after fail-fast test updates.
- `gitops-repo`: `/usr/local/bin/kubectl kustomize` passed for VSO platform, clusters, shared dependencies, and dev-1 / sta-1 target overlays.
- `vincent-k3s`: Vault server initialized/unsealed; VSO controller Ready; `VaultConnection` and 10 `VaultAuth` resources Healthy/Ready.
- `vincent-k3s`: temporary VSO probe synced dev-1 quote-api Vault path into an isolated Secret with final env keys only, then probe resources were deleted.
- `business-repo`, `gitops-repo`, `harness-repo`: `git diff --check` passed.

## Open Questions

- None for T2 platform readiness.

## Residual Risk

- T2 platform readiness is complete, but T4/T5 must not be treated as complete until service images are published and dev-1 / sta-1 cutover is applied in order.
- Vault uses standalone file storage; pod restart requires unseal with the cluster-local `vault-unseal-key` Secret.
- Live dev-1 and sta-1 rollout/smoke/trace verification has not run.
- `merge-readiness` is intentionally not created while service cutover and environment verification remain incomplete.

## Conclusion

- `platform-ready-cutover-pending`：T2 的 Vault/VSO 底座已具备 live 证据；T4/T5 仍需按镜像发布、dev-1、sta-1 顺序继续。

本报告不是门禁结论。阶段推进仍以 Janus 门禁 JSON 和人工审批为准。
