---
requirement_id: "LEN-36"
task_id: "T2-T4"
reviewer: "Codex"
base_revision: "business-repo 9d111cb; harness-repo cbfce4b"
diff_scope: "business-repo scanner/config/tests/workflow and LEN-36 evidence/gates"
conclusion: "ready-for-gate"
updated_at: "2026-06-20"
---

# Code Review Report

## Scope

- Repository: `business-repo`, `harness-repo`
- Base revision: `business-repo 9d111cb`; `harness-repo cbfce4b`
- Changed files: `scripts/contract_dependency_scan.py`, `config/contract-dependencies.json`, `.github/workflows/contract-dependency-scan.yml`, `tests/test_contract_dependency_scan.py`, `tests/contract_dependency_scan/fixtures/**`, `requirements/LEN-36/**`
- Task ID: T2, T3, T4

## Findings

| Severity | Dimension | Location | Issue | Impact | Required Fix | Status |
|---|---|---|---|---|---|---|
| P2 | 既有依赖债务 | `services/backend/user-api/pom.xml` | `user-api` 当前仍消费 `com.spark.contract:spark-idl-java:0.1.0-SNAPSHOT`。 | 本需求通过 changed-file scan 接入门禁；后续触碰该依赖文件或手动全仓扫描会被阻塞。 | 发布 formal `spark-idl-java` 契约版本后，用独立需求更新 `user-api` 依赖。 | open |

## Dimension Coverage

| Dimension | Checker | Result | Checked Scope |
|---|---|---|---|
| 追溯与范围 | code_review_traceability_checker | skipped | 未 spawn 子 agent；主线程检查 T2-T4 与 R1-R9 / D1-D7 / AC1-AC9 的追溯一致。 |
| 契约兼容 | code_review_contract_checker | skipped | 未 spawn 子 agent；主线程确认不修改 `.proto`、生成契约或业务服务契约。 |
| 数据与并发 | code_review_data_concurrency_checker | skipped | 未 spawn 子 agent；本变更无数据、事务、并发、重试或幂等行为。 |
| 安全与错误处理 | code_review_security_error_checker | skipped | 未 spawn 子 agent；主线程确认 workflow 只读权限，脚本无 secret 读取。 |
| 架构边界 | backend_architecture_reviewer | skipped | 未修改 backend 服务代码。 |
| 测试价值与复杂度 | code_review_reporter | no findings | 14 条 fixture 测试覆盖 Java / Go pass-fail 规则、输出定位字段、dependencyManagement 解析和 changed-file scan。 |

## Tests Inspected

- `python3 -m unittest tests/test_contract_dependency_scan.py` -> PASS, 14 tests.
- `python3 scripts/contract_dependency_scan.py --mode master --root tests/contract_dependency_scan/fixtures/java-dependency-management-formal-pass` -> PASS.
- `python3 scripts/contract_dependency_scan.py --mode master --path .github/workflows/contract-dependency-scan.yml --path scripts/contract_dependency_scan.py --path config/contract-dependencies.json` -> PASS.
- `python3 scripts/contract_dependency_scan.py --mode master --path services/backend/user-api/pom.xml` -> BLOCKED, expected existing dependency debt.
- `python3 scripts/contract_dependency_scan.py --mode master --root .` -> BLOCKED, manual full scan exposes existing dependency debt.

## Open Questions

- 是否在后续 ticket 中补充 PR label 自动触发 `rc` 模式。
- formal `spark-idl-java` 版本何时发布，并由哪个 ticket 更新 `user-api` 消费版本。

## Residual Risk

- Maven 父 POM / profile 跨文件版本解析首版未覆盖；当前策略 fail closed。
- Go branch dependency 没有官方单一语法；当前策略把非 formal / 非合规 RC / 非 pseudo-version 归入不可分类并阻塞。

## Conclusion

实现代码无未关闭 P0/P1，T2-T4 可进入 gate 证据阶段。`merge-readiness` 可通过本需求落地；现有 SNAPSHOT contract dependency 作为后续 formal 版本切换债务记录。
