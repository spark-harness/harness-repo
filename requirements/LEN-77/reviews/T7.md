---
requirement_id: "LEN-77"
task_id: "T7"
reviewer: "Codex"
base_revision: "origin/master"
diff_scope: "harness-repo requirements/LEN-77, business-repo fides/applicant/fides-bff runtime changes, gitops-repo lendora-sta runtime changes"
conclusion: "ready-for-gate"
updated_at: "2026-06-23T23:48:40+08:00"
---

# Code Review Report

## Scope

- Repository: `harness-repo`, `business-repo`, `gitops-repo`
- Base revision: `origin/master`
- Changed files:
  - `harness-repo/requirements/LEN-77/**`
  - `business-repo/services/backend/applicant-api/Dockerfile`
  - `business-repo/services/backend/applicant-api/src/main/resources/application-sta.yml`
  - `business-repo/services/backend/fides-bff/Dockerfile`
  - `business-repo/services/frontend/fides/Dockerfile`
  - `business-repo/services/frontend/fides/next.config.ts`
  - `business-repo/services/frontend/fides/package.json`
  - `business-repo/services/frontend/fides/pnpm-workspace.yaml`
  - `business-repo/services/frontend/fides/src/api/mobile-verification/mobile-verification-real-bff.smoke.test.tsx`
  - `gitops-repo/apps/**`
  - `gitops-repo/clusters/lendora-sta/**`
  - `gitops-repo/platform/ingress/caddy-github-webhook.yaml`
  - `gitops-repo/workflows/**`
- Task ID: T7

## Findings

| Severity | Dimension | Location | Issue | Impact | Required Fix | Status |
|---|---|---|---|---|---|---|
| P2 | Rollback | `requirements/LEN-77/evidence/runtime-smoke-plan.md` | The exercised older applicant-api digest rolled out to `CrashLoopBackOff`, then the current digest was restored. | Rollback mechanics are proven, but the tested older digest is not a durable known-good rollback target for the current STA data state. | Capture the next successful applicant-api digest as the rollback candidate in a follow-up technical-debt ticket. | open |
| P2 | Runtime config | `gitops-repo/apps/applicant-api/base/configmap.yaml` | STA uses `APPLICANT_MIGRATIONS_ENABLED=false` after schema bootstrap. | Steady-state restarts are stable, but migration ownership is not yet a durable release procedure. | Split migration bootstrap / steady-state migration policy into a follow-up ticket. | open |
| P2 | Public entry | `gitops-repo/platform/ingress/caddy-github-webhook.yaml` | Temporary public route uses `api.fuzzytails.fun` because `lendora-sta*` DNS / certificate names were unavailable. | Runtime smoke is complete, but final Lendora STA branded DNS remains unfinished. | Add DNS / certificate setup for final `lendora-*` public entries in a follow-up ticket. | open |
| P3 | Frontend package metadata | `business-repo/services/frontend/fides/pnpm-workspace.yaml` | `allowBuilds` contains pnpm approval metadata-like placeholder values for `sharp` and `unrs-resolver`. | Non-blocking for runtime closure because focused tests pass and ignored build dependencies remain declared, but it should be normalized before broader package governance. | Clean up or formally approve pnpm build metadata in a follow-up change. | open |

Severity 口径：

- `P0`：正确性、数据丢失、安全或契约破坏。
- `P1`：大概率生产 Bug、缺少必需证据或门禁阻塞项。
- `P2`：可维护性、测试、可观测性或灰度风险。
- `P3`：不应阻塞的小问题。

## Dimension Coverage

| Dimension | Checker | Result | Checked Scope |
|---|---|---|---|
| 追溯与范围 | code_review_traceability_checker | skipped | Skipped because the user did not explicitly request delegated sub-agents; main-thread review checked T7 traceability against R7, D7 and AC10-AC14. |
| 契约兼容 | code_review_contract_checker | skipped | Skipped because the user did not explicitly request delegated sub-agents; main-thread review confirmed no protobuf source or generated contract changes. |
| 数据与并发 | code_review_data_concurrency_checker | skipped | Skipped because the user did not explicitly request delegated sub-agents; main-thread review checked rollback and migration residual risks. |
| 安全与错误处理 | code_review_security_error_checker | skipped | Skipped because the user did not explicitly request delegated sub-agents; main-thread review checked public boundary, CORS, private applicant-api probe and sensitive-log evidence. |
| 架构边界 | backend_architecture_reviewer | skipped | Skipped because the user did not explicitly request delegated sub-agents; main-thread review checked applicant-api internal boundary and fides-bff public API boundary. |
| 测试价值与复杂度 | code_review_reporter | findings | Main-thread aggregation inspected focused unit/integration tests, real BFF smoke, readiness, Consul health, negative public probe, rollback exercise and evidence quality. |

`skipped` 必须附原因。

## Tests Inspected

- `mvn -q -Dtest=ConsulServiceRegistrationTest,ApplicantAuthConfigurationTest test`: PASS.
- `go test ./internal/server ./internal/conf ./cmd/fides-bff`: PASS.
- `./node_modules/.bin/vitest run src/presentation/mobile-verification/mobile-verification-screen.test.tsx src/api/mobile-verification/create-mobile-verification-controller.test.ts src/infrastructure/mobile-verification/rest-otp-auth-gateway.test.ts`: PASS, 3 files / 20 tests.
- `LEN43_REAL_BFF_SMOKE=1 LEN43_FIDES_BFF_BASE_URL=https://api.fuzzytails.fun/api/v1 LEN43_SMOKE_PHONE=91990003 ./node_modules/.bin/vitest run src/api/mobile-verification/mobile-verification-real-bff.smoke.test.tsx`: PASS, 1 file / 1 test.
- `curl -kfsS https://api.fuzzytails.fun/api/v1/health`: PASS, returned `{"status":"ok","version":"dev"}`.
- `curl -kfsSI --max-time 20 https://lendora-sta-applicant-api.fuzzytails.fun/health`: PASS as negative evidence, TLS handshake failed because no public applicant-api route exists.
- `GODEBUG=tlsmlkem=0 KUBECONFIG=$HOME/.kube/vincent-k3s.yaml kubectl get deploy ...`: PASS, applicant-api / fides-bff / fides all `1/1` Ready.
- Consul HTTP API `/v1/health/checks/applicant-api`: PASS, applicant-api service check `passing`.
- Sensitive log scan for smoke phone / OTP / token / applicantId patterns: PASS, no matches found.

## Open Questions

- Which follow-up ticket will own final `lendora-*` DNS / certificate setup?
- Which follow-up ticket will own durable STA migration lifecycle policy?
- Which successful future digest should become the documented rollback candidate after the next image release?

## Residual Risk

- Current public route is operational but not the final branded Lendora STA DNS.
- OTEL collector / trace evidence is not wired in this closure; readiness, Consul health, smoke, and log checks are the runtime evidence.
- The rollback exercise proved the mechanics and restore path, not that the tested older digest is a valid rollback target for the current database state.

## Conclusion

`ready-for-gate`: no open P0/P1 findings. Open P2/P3 items are non-blocking implementation-first follow-ups and should be tracked outside LEN-77 closure.

本报告不是门禁结论。阶段推进仍以 Janus 门禁 JSON 和人工审批为准。
