# LEN-98

`LEN-98` 承接父 Story `LEN-2` 中 FE+BFF 契约生成链路：`fides-bff` 以 protobuf HTTP annotation 作为 FE-facing API 事实来源，生成 OpenAPI v3 YAML 和 TypeScript HTTP client，并让 `fides` adapter 层消费生成 client。

## Artifacts

- `requirement.md`
- `impact-analysis.md`
- `design.md`
- `tasks.json`
- `evidence/`
